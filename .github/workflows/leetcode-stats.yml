name: Update LeetCode stats

on:
  repository_dispatch:
    types: [leetcode-submission]

  # Fallback in case the submission trigger fails.
  schedule:
    - cron: "2/5 * * * *"

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-leetcode-stats
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Gives LeetCode's API time to expose the new submission.
      - name: Wait for LeetCode
        if: github.event_name == 'repository_dispatch'
        run: sleep 15

      - name: Generate SVG
        run: node scripts/generate-leetcode-svg.mjs leetcode-stats.svg

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add leetcode-stats.svg

          git diff --staged --quiet || {
            git commit -m "chore: update LeetCode stats"
            git push
          }
